# خطوات تشغيل المنصة بعد الإصلاحات

## 1) شغّلي السيرفر من سطر الأوامر:

```bash
cd "/Users/amnah/aman education/amaneducation"
source venv/bin/activate
python manage.py migrate          # لا migrations جديدة لكن للتحقق
python manage.py runserver
```

ثم افتحي: <http://127.0.0.1:8000>

---

## 2) أحدث التغييرات (ما تم إنجازه):

### 🔐 تسجيل الدخول
- ثلاث تبويبات: **طالبة** (هوية فقط) | **معلمة** (هوية + PIN) | **مديرة** (مستخدم + كلمة مرور).
- التبويبة الافتراضية = الطالبة.
- إن أردتِ مستقبلاً تفعيل PIN لطالبة معيّنة → اضبطي `Profile.pin_code` لها وستحتاج لاستخدام تبويبة المعلمة.

### 👑 لوحة المديرة (`/admin-dashboard/`)
- ٥ تبويبات: المعلمات / الطالبات / الفصول / إضافة / رفع Excel.
- إضافة/تعديل/حذف معلمة وطالبة.
- رفع Excel للمعلمات (الأعمدة: الاسم، رقم الهوية، PIN اختياري).
- رفع Excel للطالبات (الأعمدة: الاسم، رقم الهوية، الفصل).
- إدارة الفصول (إضافة/حذف).
- زر تعديل لكل مستخدم (تغيير الاسم، PIN، تفعيل/تعطيل).

### 🏆 الاختبارات الشاملة (`/admin-dashboard/comprehensive/`)
- إضافة اختبار شامل (قدرات/تحصيلي).
- خيار **"جميع الفصول"** للتفعيل.
- زر **🖨️ طباعة** يفتح نسخة قابلة للطباعة (مع/بدون إجابات).
- زر **🔗 رابط** + زر **💬 واتساب** لإرسال الرابط مباشرة.
- زر **⚙️ تعديل** و **🗑️ حذف**.
- زر **📥 Excel** لرفع الأسئلة.

### 🏠 الصفحة الرئيسية
- زر "🏆 الاختبارات الشاملة" → عرض خيارين (قدرات/تحصيلي) → بطاقات الاختبارات.
- إصلاح فلتر الـ JS (كان يُخفي بطاقات الاختبارات الشاملة).
- صلاحيات: الطالبة لا ترى بنوك الأسئلة ولا اختبارات التحصيلي.

### 👩‍🏫 لوحة المعلمة
- زر **📖 عرض** لكل مهارة → صفحة كاملة مع المحتوى والأسئلة وزر طباعة.
- زر **✏️ تعديل** → صفحة تعديل (عنوان/وصف/محتوى/فيديو/PDF/خاص-مشترك).
- زر **🗑️ حذف**.
- إضافة/تعديل/حذف الأسئلة فردياً (`edit_question`, `delete_question`).
- رفع Excel للأسئلة.

### 👩‍🎓 الطالبة
- داشبورد بإحصاءات (اختبارات أُديت/ناجحة/متوسط).
- اختبارات متاحة (المفعّلة لها فقط).
- نتائجها السابقة مع تفاصيل.
- لا تظهر لها بنوك الأسئلة ولا اختبارات التحصيلي في الواجهة.

### 🖨️ الطباعة
- `/skill/<id>/print/` لطباعة محتوى المهارة.
- `/exam/<id>/print/` لطباعة الاختبار:
  - مع `?answers=1` (للمعلمة، مع الإجابات الصحيحة بلون أخضر).
  - بدون `answers` افتراضياً للطالبة.
- زر طباعة موجود في:
  - `home.html` (محتوى الصفحة الرئيسية).
  - `skill_detail.html` (تبويبات القبلي/البعدي).
  - `admin_comprehensive.html` (لكل اختبار شامل).
  - `view_skill.html` (لكل اختبار مخصص).

### 📊 الإحصائيات
- لوحة المديرة: KPI + جداول حية بحساب select_related/annotate (لا N+1).
- لوحة المعلمة: تصنيف الطالبات (متفوّقات/متوسطات/ضعيفات) من النتائج الفعلية.
- لوحة الطالبة: نسبة النجاح + متوسط الدرجات.

---

## 3) النسخة الاحتياطية:

تم أخذ نسخة احتياطية من `db.sqlite3` باسم `db.sqlite3.backup_<timestamp>`.

---

## 4) إن واجهتِ مشكلة:

- شغّلي `python manage.py check` للتحقق من سلامة الإعدادات.
- لو ظهر خطأ في URL، تأكدي من حفظ كل الملفات (Cmd+S في المحرر).
- لو لم تظهر الزرّ في الصفحة، أعيدي تحميل الصفحة بـ `Cmd+Shift+R` لتجاوز الكاش.

---

## 5) ملفات Excel نموذجية:

### نموذج المعلمات:
| الاسم الكامل | رقم الهوية | PIN |
|--|--|--|
| فاطمة محمد | 1012345678 | 1234 |

### نموذج الطالبات:
| الاسم | رقم الهوية | الفصل |
|--|--|--|
| نورة أحمد | 1098765432 | ث١٢ |

### نموذج الأسئلة:
| السؤال | أ | ب | ج | د | الصحيحة | المهارة | تعليق |
|--|--|--|--|--|--|--|--|
| ٢ + ٣ = ؟ | 4 | 5 | 6 | 7 | B | الجمع | الإجابة 5 |
